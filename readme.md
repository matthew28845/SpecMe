![SpecMe logo](images/specmebanner.png)

SpecMe is a free and open-source system specification viewer written in Electron. It will run on Windows, macOS, and Linux.

To run:
1. `npm install`
2. `npm run start`
3. 
To compile:
1. `npm install`
2. `npm run make`